<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<!-- Converted by suse-upgrade version 1.1 -->
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook50-profile.xsl" 
 type="text/xml"
 title="Profiling step"?>
<!-- z: s1-sm-errata ref.webui.patches -->
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="ref.webui.patches">
 <title>Patches</title>
 <info/>
<!-- idx --><indexterm>
 <primary>Patches</primary></indexterm><indexterm>
 <primary>WebLogic</primary>
 <secondary>Patches</secondary></indexterm>
 <para>
  The <guimenu>Patches</guimenu> menu from the left bar helps tracking the
  availability and application of patches to your managed systems.
 </para>
 <para>
  The <menuchoice> <guimenu>Patches</guimenu> <guimenu>Patches</guimenu>
  </menuchoice> page displays all or relevant patches for at least one of your
  managed systems that have not been applied yet.
 </para>
 <note>
  <title>Receiving Patches for Your System</title>
  <para>
   To receive an e-mail when patches are issued for your system, go to
   <menuchoice> <guimenu>Overview</guimenu> <guimenu>Your Preferences</guimenu>
   </menuchoice> and select <guimenu>Receive email notifications</guimenu>.
  </para>
 </note>
 <para>
  &suse; distinguishes three types of patches: security updates, bug fix
  updates, and enhancement updates. Each patch consists of a summary of the
  problem and solution, including the RPM packages fixing the problem.
 </para>
 <para>
  Icons are used to identify the three types:
 </para>
 <itemizedlist mark="bullet" spacing="normal">
  <listitem>
   <para>
    <inlinemediaobject>
    <textobject role="description"><phrase>Security Alert</phrase>
    </textobject>
    <imageobject role="fo">
     <imagedata fileref="fa-shield.svg" width="1.2em" format="SVG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="fa-shield.png" width="1em" format="PNG"/>
    </imageobject>
    </inlinemediaobject> — Security Updates available,
    <emphasis>strongly</emphasis> recommended
   </para>
  </listitem>
  <listitem>
   <para>
    <inlinemediaobject>
    <textobject role="description"><phrase>Bug Fix Alert</phrase>
    </textobject>
    <imageobject role="fo">
     <imagedata fileref="fa-bug.svg" width="1.6em" format="SVG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="fa-bug.png" width="1em" format="PNG"/>
    </imageobject>
    </inlinemediaobject> — Bug Fix Updates available, recommended
   </para>
  </listitem>
  <listitem>
   <para>
    <inlinemediaobject>
    <textobject role="description"><phrase>Enhancement Alert</phrase>
    </textobject>
    <imageobject role="fo">
     <imagedata fileref="spacewalk-icon-enhancement.svg" width="1.4em" format="SVG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="spacewalk-icon-enhancement.png" width="1em" format="PNG"/>
    </imageobject>
    </inlinemediaobject> — Enhancement Updates available, optional
   </para>
  </listitem>
 </itemizedlist>
 <para>
  A summary of each patch is provided in list form displaying its type,
  advisory ID, synopsis (with the severity as a textual prefix in case of
  security updates, such as <quote>critical</quote>, <quote>important</quote>,
  <quote>moderate</quote>, or <quote>low</quote>), number of affected systems
  in your network, and date updated.
 </para>
 <para>
  In addition, you may view patches by product line at the following location:
  <link
   xlink:href="http://download.suse.com/patch/psdb/"/>. For more
  information on security updates, see
  <link xlink:href="https://www.suse.com/support/security/"/>.
 </para>
<!-- z: s2-sm-applicable-errata ref.webui.patches.relevant -->
 <sect1 xml:id="ref.webui.patches.relevant">
  <title>Relevant</title><indexterm>

  <primary>Web UI</primary>

  <secondary>Relevant Patches</secondary></indexterm><indexterm>

  <primary>Web UI</primary>

  <secondary>viewing list of applicable patches</secondary></indexterm><indexterm>

  <primary>WebLogic</primary>

  <secondary>Relevant Patches</secondary></indexterm>

  <para>
   The <guimenu>Relevant</guimenu> patches page displays a customized list of
   patches applying to your registered systems.
  </para>

  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="patches_relevant_patches.png" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="patches_relevant_patches.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>

  <para>
   Clicking an <guimenu>Advisory</guimenu> ID of a patch takes you to the
   <guimenu>Details</guimenu> page of the <guimenu>Patch Details</guimenu>
   page. Clicking the number of associated systems takes you to the
   <guimenu>Affected Systems</guimenu> page of the <guimenu>Patch
   Details</guimenu> page. Refer to <xref linkend="s3-sm-errata-details"/> for
   more information.
  </para>
 </sect1>
<!-- z: s2-sm-all-errata ref.webui.patches.all -->
 <sect1 xml:id="ref.webui.patches.all">
  <title>All</title>

<!-- idx --><indexterm>

  <primary>Web UI</primary>

  <secondary>All Patches</secondary></indexterm><indexterm>

  <primary>Web UI</primary>

  <secondary>viewing list of all patches</secondary></indexterm><indexterm>

  <primary>WebLogic</primary>

  <secondary>All Patches</secondary></indexterm>

  <para>
   The <guimenu>All</guimenu> patches page displays a list of all patches
   released by &suse;, irrelevant of whether they apply to your registered
   systems or not.
  </para>

  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="patches_all_patches.png" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="patches_all_patches.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>

  <para>
   Like in the <guimenu>Relevant Patches</guimenu> page, clicking either
   <guimenu>Advisory</guimenu> or the number of systems affected takes you to
   related tabs of the <guimenu>Patch Details</guimenu> page. Refer to
   <xref linkend="s3-sm-errata-details"/> for more information.
  </para>

  <sect2 xml:id="s3-sm-apply-errata">
   <title>Applying Patches</title><indexterm>
   <primary>Web UI</primary>
   <secondary>applying patches</secondary></indexterm>
   <para>
    Patches include a list of updated packages. To apply patches to a system,
    the system must be entitled.
   </para>
   <para>
    Apply all applicable patches to a system by clicking <menuchoice>
    <guimenu>Systems</guimenu> <guimenu>Systems</guimenu> </menuchoice> in the
    top and left navigation bars. Click the name of an entitled system. Then
    in the <guimenu>System Details</guimenu> page click the <menuchoice>
    <guimenu>Software</guimenu> <guimenu>Patches</guimenu> </menuchoice>
    subtab. When the relevant patch list appears, click <guimenu>Select
    All</guimenu> then <guimenu>Apply Patches</guimenu> on the bottom
    right-hand corner of the page. Only patches not scheduled, scheduled but
    failed, or canceled patches are listed. Pending updates are excluded.
   </para>
   <para>
    In addition, users with appropriate roles can apply patches using two other
    methods:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      To apply a specific patch to one or more systems, locate it in the patch
      list and click the number of systems affected, which takes you to the
      <guimenu>Affected Systems</guimenu> page of the <guimenu>Patch
      Details</guimenu> page. Select the individual systems to be updated and
      click the <guimenu>Apply Patches</guimenu> button. Double-check the
      systems to be updated on the confirmation page, then click the
      <guimenu>Confirm</guimenu> button.
     </para>
    </listitem>
    <listitem>
     <para>
      To apply more than one patch to one or more systems, select the systems
      from the <guimenu>Systems</guimenu> list. Click the <guimenu>System Set
      Manager</guimenu> link in the left navigation bar, then click the
      <guimenu>Systems</guimenu> tab. After ensuring the appropriate systems
      are selected, click the <guimenu>Patches</guimenu> tab, select the
      patches to apply, and click the <guimenu>Apply Patches</guimenu> button.
      Schedule a date and time for the patch to be applied. Default is the
      current date. Click the <guimenu>Confirm</guimenu> button. You can follow
      the progress of the patch application via the <guimenu>Pending
      Actions</guimenu> list. Refer to <xref linkend="ref.webui.schedule"/> for
      more details.
     </para>
    </listitem>
   </itemizedlist>
   <important>
    <para>
<!-- FIXME: is this the same as "Auto Patch Update"? -->
     If you use scheduled package installation, the packages or patches are
     installed via the &susemgr; daemon (<command>rhnsd</command>). You must
     enable the &susemgr; daemon on your systems. For more information about
     the &susemgr; daemon, see <xref linkend="bp.contact.methods.rhnsd"/>.
    </para>
   </important>
   <para>
    The following rules apply to patches:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Each package is a member of one or more channels. If a selected system is
      not subscribed to a channel containing the package, the update will not
      be installed on that system.
     </para>
    </listitem>
    <listitem>
     <para>
      If a newer version of the package is already installed on the system, the
      update will not be installed.
     </para>
    </listitem>
    <listitem>
     <para>
      If an older version of the package is installed, the package will be
      upgraded.
     </para>
    </listitem>
   </itemizedlist>
  </sect2>

  <sect2 xml:id="s3-sm-errata-details">
   <title>Patch Details</title><indexterm>
   <primary>Web UI</primary>
   <secondary>viewing details</secondary></indexterm>
   <para>
    If you click the advisory of a patch in the <guimenu>Relevant</guimenu>
    or <guimenu>All</guimenu> pages, its <guimenu>Patch Details</guimenu> page
    appears. This page is further divided into the following tabs:
   </para>
   <sect3 xml:id="s4-sm-errata-details-details">
<!--status="rfc"-->
    <title><guimenu>Patch Details</guimenu> &gt; <guimenu>Details</guimenu></title>
    <para>
     This subtab displays the patch report issued by &suse;. It provides a
     synopsis of the patch first (with the severity as a textual prefix in case
     of security updates, such as <quote>critical</quote>,
     <quote>important</quote>, <quote>moderate</quote>, or <quote>low</quote>),
     issue date, and any update dates. This is followed by a description of the
     patch and the steps required to resolve the issue.
    </para>
    <para>
     Below the <guimenu>Affected Channels</guimenu> label, all channels that
     contain the affected package are listed. Clicking a channel name
     displays the <guimenu>Packages</guimenu> subtab of the <guimenu>Channel
     Details</guimenu> page for that channel. Refer to
     <xref linkend="s3-sm-channel-details"/> for more information.
    </para>
<!--<para>
     Below <guilabel>Fixes</guilabel>, the specific Bugzilla entries
     resolved by this patch are listed. Clicking any summary text opens
     that Bugzilla entry at <ulink url="http://bugzilla.redhat.com"/>. Note
     that you must have a Bugzilla account to view the entry.
    </para>-->
    <para>
     Security updates list the specific vulnerability as tracked by
     <link
      xlink:href="http://cve.mitre.org"/>. This information is
     listed below the <guimenu>CVEs</guimenu> label.
    </para>
<!-- bnc#661025 -->
    <para>
<!--&suse; provides security update information in OVAL format.-->
     OVAL is an open vulnerability and assessment language promoted by Mitre,
     <link
      xlink:href="http://oval.mitre.org"/>. Clicking the link
     below the <guimenu>Oval</guimenu> label downloads this information to your
     system. More useful are the &suse; Update Advisories at
     <link xlink:href="https://www.suse.com/support/update/"/>.
    </para>
   </sect3>
   <sect3 xml:id="s4-sm-errata-details-packages">
    <title><guimenu>Patch Details</guimenu> &gt; <guimenu>Packages</guimenu></title>
    <para>
     This page provides links to each of the updated RPMs by channel. Clicking
     the name of a package displays its <guimenu>Package Details</guimenu>
     page.
    </para>
   </sect3>
   <sect3 xml:id="s4-sm-errata-details-systems">
    <title><guimenu>Patch Details</guimenu> &gt; <guimenu>Affected Systems</guimenu></title>
    <para>
     This page lists systems affected by the patches. You can apply updates
     here. (See <xref
      linkend="s3-sm-apply-errata"/>.) Clicking the
     name of a system takes you to its <guimenu>System Details</guimenu> page.
     Refer to <xref linkend="s3-sm-system-details"/> for more information.
    </para>
    <para>
     To determine whether an update has been scheduled, refer to the
     <guimenu>Status</guimenu> column in the affected systems table. Possible
     values are: N/A, Pending, Picked Up, Completed, and Failed. This column
     identifies only the last action related to a patch. For example, if an
     action fails and you reschedule it, this column shows the status of the
     patch as pending with no mention of the previous failure. Clicking a
     status other than <guimenu>N/A</guimenu> takes you to the <guimenu>Action
     Details</guimenu> page. This column corresponds to one on the
     <guimenu>Patch</guimenu> tab of the <guimenu>System Details</guimenu>
     page.
    </para>
   </sect3>
  </sect2>
 </sect1>
<!-- z: s2-sm-errata-search ref.webui.patches.search -->
 <sect1 xml:id="ref.webui.patches.search">
<!--status="rfc"-->

  <title>Advanced Search</title><indexterm>

  <primary>Web UI</primary>

  <secondary>Advanced Search</secondary></indexterm><indexterm>

  <primary>Web UI</primary>

  <secondary>searching</secondary></indexterm><indexterm>

  <primary>WebLogic</primary>

  <secondary>Patch Search</secondary></indexterm>

  <para>
   The <guimenu>Patches Search</guimenu> page allows you to search through
   patches by specific criteria.
  </para>

  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="patches_advanced_search_patches.png" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="patches_advanced_search_patches.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>

  <itemizedlist mark="bullet" spacing="normal">
   <listitem>
    <para>
     <guimenu>All Fields</guimenu> — Search patches by synopsis, description,
     topic, or solution.
    </para>
   </listitem>
   <listitem>
    <para>
     <guimenu>Patch Advisory</guimenu> — The name or the label of the patch.
<!-- this no longer works:
          The &suse; security team
          codifies advisories in the following way:
     -->
    </para>
<!--
          <screen>SUSE-RU-2011:0030</screen>
          <remark>jw, 2011-02-17: RU=recommended update, SU=security update, and ??;
          FIXME: The GUI still shows Example: 'kernel', 'RHBA-2007:0113'</remark>
          <remark>ke 2011-03-25: reported as Bug 682601.</remark>
    <para>
     Searches can be done by year (such as 2011), by type of advisory, or
     full advisory name as in the example above.
     </para>
     -->
   </listitem>
   <listitem>
    <para>
     <guimenu>Package Name</guimenu> — Search particular packages by name:
    </para>
<screen>kernel</screen>
    <para>
     Results will be grouped by advisory. For example, searching for 'kernel'
     returns all package names containing the string <literal>kernel</literal>,
     grouped by advisory.
    </para>
   </listitem>
   <listitem>
    <para>
     <guimenu>CVE</guimenu> — The name assigned to the security advisory by
     the Common Vulnerabilities and Exposures (CVE) project at
     <link xlink:href="http://cve.mitre.org"/>. For example:
    </para>
<screen>CVE-2006-4535</screen>
   </listitem>
  </itemizedlist>

  <para>
   To filter patch search results, check or uncheck the boxes next to the type
   of advisory:
  </para>

  <itemizedlist mark="bullet" spacing="normal">
   <listitem>
    <para>
     Bug Fix Advisory — Patches that fix issues reported by users or
     discovered during development or testing.
    </para>
   </listitem>
   <listitem>
    <para>
     Security Advisory — Patches fixing a security issue found during
     development, testing, or reported by users or a software security clearing
     house. A security advisory usually has one or more CVE names associated
     with each vulnerability found in each package.
    </para>
   </listitem>
   <listitem>
    <para>
     Product Enhancement Advisory — Patches providing new features, improving
     functionality, or enhancing performance of a package.
    </para>
   </listitem>
<!-- FIXME: add date and fine grained -->
  </itemizedlist>
 </sect1>
<!-- z: s2-sm-errata-manage ref.webui.patches.manage -->
 <sect1 xml:id="ref.webui.patches.manage">
  <title>Manage Patches</title><indexterm>

  <primary>Web UI</primary>

  <secondary>Managing Patches</secondary></indexterm>

  <para>
   Custom patches enable organizations to issue patch alerts for the packages
   in their custom channels, schedule deployment and manage patches across
   organizations.
  </para>

  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="patches_published_patches.png" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="patches_published_patches.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>

  <warning>
   <para>
    If the organization is using both &susemgr; and &susemgr; Proxy
    server, then manage patches only on the &susemgr; server since the
    proxy servers receive updates directly from it. Managing patches on
    a proxy in this combined configuration risks putting your servers
    out of synchronization.
   </para>
  </warning>

  <sect2 xml:id="s3-sm-errata-create">
   <title>Creating and Editing Patches</title><indexterm>
   <primary>Web UI</primary>
   <secondary>Creating Patches</secondary></indexterm><indexterm>
   <primary>Web UI</primary>
   <secondary>Editing Patches</secondary></indexterm>
   <para>
    To create a custom patch alert, proceed as follows:
   </para>
   <procedure>
    <step>
     <para>
      On the top navigation bar, click <guimenu>Patches</guimenu>, then
      select <guimenu>Manage Patches</guimenu> on the left navigation bar. On
      the <guimenu>Patches Management</guimenu> page, click <guimenu>Create
      Patch</guimenu>.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="patches_create_patch.png" width="80%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="patches_create_patch.png" width="80%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
    <step>
     <para>
      Enter a label for the patch in the <guimenu>Advisory</guimenu> field,
      ideally following a naming convention adopted by your organization.
      <remark role="needinfo">emap: Similar patch
       beginnings for SUSE? "Note that this label cannot begin with the letters "RH" (capitalized or
       not) to prevent confusion between custom errata and those issued by Red Hat."</remark>
     </para>
    </step>
    <step>
     <para>
      Complete all remaining required fields, then click the <guimenu>Create
      Patch</guimenu> button. View standard SUSE Alerts for examples of
      properly completed fields.
     </para>
    </step>
   </procedure>
   <para>
    Patch management distinguishes between published and unpublished patches.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      <guimenu>Published</guimenu>: this page displays the patch alerts
      the organization has created and disseminated. To edit an existing
      published patch, follow the steps described in <xref
      linkend="s3-sm-errata-create"/>. To distribute the patch, click
      <guimenu>Send Notification</guimenu> in the <guimenu>Send Patch
      Mail</guimenu> section on the top of the <guimenu>Patch
      Details</guimenu> page. The patch alert is sent to the
      administrators of all affected systems.
     </para>
    </listitem>
    <listitem>
     <para>
      <guimenu>Unublished</guimenu>: this page displays the patch alerts
      your organization has created but not yet distributed. To edit an
      existing unpublished patch, follow the steps described in <xref
      linkend="s3-sm-errata-create"/>. To publish the patch, click
      <guimenu>Publish Patch</guimenu> on the top-right corner of the
      <guimenu>Patch Details</guimenu> page. Confirm the channels
      associated with the patch and click the <guimenu>Publish
      Patch</guimenu> button, now in the lower-right corner. The patch
      alert is moved to the <guimenu>Published</guimenu> page awaiting
      distribution.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    &susemgr; administrators can also create patches by cloning an existing
    one. Cloning preserves package associations and simplifies issuing patches.
    See <xref
     linkend="ref.webui.patches.clone"/> for instructions.
   </para>
   <para>
    To edit an existing patch alert's details, click its advisory on the
    <guimenu>Patches Management</guimenu> page, make the changes in the
    appropriate fields of the <guimenu>Details</guimenu> tab, and click the
    <guimenu>Update Patch</guimenu> button. Click the
    <guimenu>Channels</guimenu> tab to alter the patch's channel association.
    Click the <guimenu>Packages</guimenu> tab to view and modify its
    packages.
   </para>
   <para>
    To delete patches, select their check boxes on the <guimenu>Patches
    Management</guimenu> page, click the <guimenu>Delete Patches</guimenu>
    button, and confirm the action. Deleting published patches might take a few
    minutes.
   </para>
  </sect2>

  <sect2 xml:id="s3-sm-errata-assign-packs">
   <title>Assigning Packages to Patches</title>
   <para>
    To assign packages to patches, proceed as follows:
   </para>
   <procedure>
    <step>
     <para>
      Select a patch, click the <guimenu>Packages</guimenu> tab, then the
      <guimenu>Add</guimenu> subtab.
     </para>
    </step>
    <step>
     <para>
      To associate packages with the patch being edited, select the channel
      from the <guimenu>View</guimenu> drop-down box that contains the
      packages and click <guimenu>View</guimenu>. Packages already associated
      with the patch being edited are not displayed. Selecting <guimenu>All
      managed packages</guimenu> presents all available packages.
     </para>
    </step>
    <step>
     <para>
      After clicking <guimenu>View</guimenu>, the package list for the selected
      option appears. Note that the page header still lists the patch being
      edited.
     </para>
    </step>
    <step>
     <para>
      In the list, select the check boxes of the packages to be assigned to the
      edited patch and click <guimenu>Add Packages</guimenu> at the
      bottom-right corner of the page.
     </para>
    </step>
    <step>
     <para>
      A confirmation page appears with the packages listed. Click
      <guimenu>Confirm</guimenu> to associate the packages with the patch. The
      <guimenu>List/Remove</guimenu> subtab of the <guimenu>Managed Patch
      Details</guimenu> page appears with the new packages listed.
     </para>
    </step>
   </procedure>
   <para>
    When packages are assigned to a patch, the patch cache is updated to
    reflect the changes. This update is delayed briefly so that users may
    finish editing a patch before all the changes are made available. To
    initiate the changes to the cache manually, follow the directions to
    <guimenu>commit the changes immediately</guimenu> at the top of the page.
   </para>
  </sect2>

  <sect2 xml:id="s3-sm-errata-publish">
   <title>Publishing Patches</title><indexterm>
   <primary>Web UI</primary>
   <secondary>Publishing Patches</secondary></indexterm>
   <para>
    After adding packages to the patch, the patch needs to be published to be
    disseminated to affected systems. Follow this procedure to publish patches:
   </para>
   <procedure>
    <step>
     <para>
      On the top navigation bar, click <guimenu>Patches</guimenu>, then
      <menuchoice><guimenu>Manage
      Patches</guimenu><guimenu>Unpublished</guimenu></menuchoice> on
      the left navigation bar to see all the unpublished patches listed.
     </para>
    </step>
    <step>
     <para>
      Click the patch <guimenu>Advisory</guimenu> name to open the patch
      details pages.
     </para>
    </step>
    <step>
     <para>
      On the patch details page, click <guimenu>Publish
      Patch</guimenu>. A confirmation page appears that will ask you to
      select which channels you want to make the patch available
      in. Choose the relevant channels.
     </para>
    </step>
    <step>
     <para>
      At the bottom of the page, click <guimenu>Publish
      Patch</guimenu>. The patch published will now appear on the
      <guimenu>Published</guimenu> page of <guimenu>Manage
      Patches</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>
  <sect2 xml:id="s3-sm-errata-published">
   <!-- FIXME: add a reference back to the management procedure; provide
        more details
   -->
   <title>Published</title><indexterm>
   <primary>Web UI</primary>
   <secondary>Published Patches</secondary></indexterm>
   <para>
    Here all published patches are listed.  It is possible to perform
    the following actions:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      To create a patch, click <guimenu>Create Patch</guimenu>.
     </para>
    </listitem>
    <listitem>
     <para>
      To delete patches, select them first and then click
      <guimenu>Delete Patches</guimenu>.
     </para>
    </listitem>
    <listitem>
     <para>
      Click an Advisory name to open the patch details page.
     </para>
    </listitem>
   </itemizedlist>
  </sect2>
  <sect2 xml:id="s3-sm-errata-unpublished">
   <title>Unpublished</title><indexterm>
   <primary>Web UI</primary>
   <secondary>Unpublished Patches</secondary></indexterm>
   <para>
    Here all published patches are listed.  It is possible to perform
    the same actions as with published patches.  For more information,
    see <xref linkend="s3-sm-errata-published"/>.  Additionally, on a
    patch details page, you can click <guimenu>Publish Patch</guimenu>
    for publishing.
   </para>

  </sect2>
 </sect1>
<!-- z: s3-sm-errata-clone ref.webui.patches.clone -->
 <sect1 xml:id="ref.webui.patches.clone">
  <title>Cloning Patches</title><indexterm>

  <primary>Web UI</primary>

  <secondary>Cloning Patches</secondary></indexterm>

  <para>
   Patches can be cloned for easy replication and distribution as part of
   &susemgr;.
  </para>

  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="patches_clone_patches.png" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="patches_clone_patches.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>

  <para>
   Only patches potentially applicable to one of your channels can be cloned.
   Patches can be applicable to a channel if that channel was cloned from a
   channel to which the patch applies. To access this functionality, click
   <guimenu>Patches</guimenu> on the top navigation bar, then <guimenu>Clone
   Patches</guimenu> on the left navigation bar.
  </para>

  <para>
   On the <guimenu>Clone Patches</guimenu> page, select the channel containing
   the patch from the <guimenu>View</guimenu> drop-down box and click
   <guimenu>View</guimenu>. When the patch list appears, select the check box
   of the patch to be cloned and click <guimenu>Clone Patch</guimenu>. A
   confirmation page appears with the patch listed. Click
   <guimenu>Confirm</guimenu> to finish cloning.
  </para>

  <para>
   The cloned patch appears in the <guimenu>Unpublished</guimenu> patch list.
   Verify the patch text and the packages associated with that patch, then
   publish the patch so it is available to users in your organization.
  </para>
 </sect1>
</chapter>
