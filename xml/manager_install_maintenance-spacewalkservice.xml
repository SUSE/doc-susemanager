<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<!-- Converted by suse-upgrade version 1.1 -->

<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="s1-maintenance-rhn-satellite">
  <title>Managing &susemgr; with spacewalk-service</title><indexterm>

  <primary>spacewalk-service</primary></indexterm>

  <para>
   Since &susemgr; consists of a multitude of individual components,
   &suse; provides the command-line tool
   <command>spacewalk-service</command> which allows you to stop, start, or
   retrieve status information from the various services in the appropriate
   order. This tool accepts all typical commands:
  </para>

<screen>/usr/sbin/spacewalk-service start
/usr/sbin/spacewalk-service stop
/usr/sbin/spacewalk-service restart
/usr/sbin/spacewalk-service reload
/usr/sbin/spacewalk-service enable
/usr/sbin/spacewalk-service disable
/usr/sbin/spacewalk-service status</screen>

  <para>
   Use <command>spacewalk-service</command> to shut down and bring up the
   entire &susemgr; and retrieve status messages from all of its services at
   once.
  </para>
  <para>In case you need to do a database schema upgrade, do the
    following:</para>
  <procedure xml:id="pro.maintenance-spacewalk-service.schema">
    <title>Performing a Database Schema Upgrade</title>
    <step>
      <para>Stop &susemgr; with <command>spacewalk-service
        stop</command>.</para>
    </step>
    <step>
      <para>Run <command>spacewalk-schema-upgrade</command>.</para>
    </step>
    <step>
      <para>Restart &susemgr; with <command>spacewalk-service start</command>.</para>
    </step>
  </procedure>
  
  
</sect1>
